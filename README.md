# NestMatch

NestMatch is a full-stack Flask + MongoDB smart living platform for roommate compatibility and swipe-based property discovery.

## Setup
1. Create and activate a Python virtual environment.
2. Install dependencies with `pip install -r requirements.txt`.
3. Ensure MongoDB is running (`mongodb://localhost:27017`).
4. Run `python app.py` and open `http://localhost:5000`.
